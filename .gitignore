.env
.env.local
.env.*.local

# Secrets integrasi lokal (isi manual; jangan commit)
macfyi-local.env
**/macfyi-local.env

# Dependencies & build
node_modules/
dist/
src-tauri/target/
*.log
.DS_Store

# Vercel (landing)
**/.vercel

# Supabase CLI cache lokal (jangan commit)
supabase/.temp/

# Secret Supabase untuk `supabase secrets set --env-file` (jangan commit)
scripts/env.supabase.secrets
.env.supabase.secrets

# Local AI runtime binaries (downloaded by scripts/fetch-llama-sidecar.sh before build)
src-tauri/resources/llama/*
!src-tauri/resources/llama/.gitkeep
