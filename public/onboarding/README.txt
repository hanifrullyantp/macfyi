Optional tour images (PNG/WebP):
  slide-0.png … slide-5.png

Place files here to replace the default gradient + icon on each onboarding slide.
If a file is missing, the app uses a built-in fallback.
